#include <stdlib.h>
#include <stdio.h>
#include <tree_node.h>
#include <error.h>

int main (int argc, char * [] argv) {
  char string [1024] = {'\0'};
  int ret = 0;
  int i = 0;

  scanf ("%s", string);
  printf ("%s\n\r", string);

  while (string [i] != '\0') {

  }

  return EC_OK;
}
